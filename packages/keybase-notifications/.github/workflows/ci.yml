name: 'ci'

on:
  - 'push'
  - 'pull_request'

jobs:
  test:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: 'actions/checkout@v1'
      - run: './build_and_test.sh'
      - uses: ./
        with:
          milliseconds: 1000
